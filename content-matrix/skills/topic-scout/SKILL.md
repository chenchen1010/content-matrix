# 选题侦察

从小红书搜索同类爆款内容，通过 4 轮迭代关键词扩展，输出 A1-A5 用户决策阶段标注的完整内容情报：客户画像表、长尾关键词库、对标爆款笔记库、选题库。

## 何时使用

当用户说"帮我找选题"、"搜一下爆款"、"看看最近什么内容火"、"找灵感"时使用此 skill。

## 前置条件

小红书 MCP 已安装并运行：
```bash
mcporter list 2>/dev/null | grep -i xiaohongshu || echo "❌ 小红书 MCP 未启动，请参考 skills/tools/search/xiaohongshu/README.md"
```

如显示 `offline`，不要把终端命令复制给用户。应：
1. 运行 **platform-login** skill，由助手执行登录脚本，或提示用户 **Cmd+Shift+P → Tasks: Run Task →「内容矩阵 · 小红书登录」**
2. 登录完成后由助手启动 `xiaohongshu-mcp`（任务：**「内容矩阵 · 启动小红书 MCP」**）

## A层分类表（A0-A5）

每条内容和每个关键词，按信号词判断所属 A层：

| A层 | 含义 | 信号词（关键词或标题含以下词） |
|-----|------|-------------------------------|
| **A0** | 完全不相关 | 不符合任何 A1-A5 信号 → **过滤，不存档** |
| **A1** | 认知启蒙 | 泛词，不含品类认知（如"家里脏了怎么办"、"家政是什么"、行业大词无修饰） |
| **A2** | 研究阶段 | 好不好、怎么样、有没有必要、值不值得、需不需要、效果、入门、了解、知道 |
| **A3** | 对比选择 | 怎么选、对比、哪家好、推荐、哪个好、测评、排行、选哪个 |
| **A4** | 决策顾虑 | 避坑、踩坑、靠不靠谱、投诉、差评、黑名单、坑、骗、后悔、注意 |
| **A5** | 准备成交 | 品牌词 + 价格、预约、电话、多少钱、怎么联系、地址、报名 |

> **规则**：差评区/评论区中提取的关键词**始终标注为 A4**，无论词面如何。
>
> **默认值**：Round 1-2 中无法判断的词默认 A2；Round 3-4 中无法判断的词默认 A3。

---

## 工作流程

### Step 1: 确认搜索方向

向用户确认：
```
需要你告诉我：
1. 你的行业/业务是什么？（如：同城家政、月嫂、海外代购）
2. 你的目标城市/地区？（如：上海、海外华人）
3. 初始关键词？（1-3 个大词，如："月嫂"、"保洁"）
```

平台固定为**小红书**。

---

### Step 2: 4 轮迭代搜索

#### 内部数据结构

```
关键词池：       本轮待搜索的关键词列表
已搜关键词集：   所有已完成搜索的关键词（去重用）
长尾关键词库：   {词: {a_layer, search_round, source}}
爆款笔记暂存：   {a_layer: [笔记列表]}（每层最多 20 条，信息饱和后停止）
```

#### 搜索执行器（3 级降级）

每搜一个关键词，按顺序尝试，成功即停：

```
Level 1: xiaohongshu-mcp（优先）
  mcporter call 'xiaohongshu.search_feeds(keyword: "{关键词}")'

Level 2: Jina Reader
  curl -s "https://r.jina.ai/https://www.xiaohongshu.com/search_result?keyword={URL编码关键词}" \
    -H "Accept: text/plain" -H "X-Return-Format: text"
  （结果 < 50 字符或含 SecurityCompromiseError → 降级）

Level 3: cursor-ide-browser
  browser_navigate → browser_wait_for（最多 10 秒）→ browser_snapshot → 提取标题和点赞
  （页面要求登录 → 提示用户在浏览器中登录小红书）
```

#### 轮次循环

```
初始化：
  关键词池 = [用户提供的初始关键词]
  已搜关键词集 = {}

Round 1 → Round 4（逐轮执行）：

  1. 对关键词池中每个词执行搜索
  2. 对每条搜索结果：
     a. 按 A层分类表 判断 A层
     b. 若 A层 = A0 → 跳过
     c. 若 A层 = A1-A5 → 暂存到 爆款笔记暂存[a_layer]（信息饱和则跳过，见 Step 3）
     d. 从标题/正文/标签提取新词 → 候选词池
     e. 挖掘差评区/评论区（见下方）→ 提取 A4 词
  3. 新词 去重（剔除 已搜关键词集 中已有的词）→ 本轮新增词
  4. 将本轮搜索词移入 已搜关键词集
  5. 将本轮新增词加入下一轮关键词池

  停止条件（任一满足即停止迭代）：
  - 本轮新增词数量 / 本轮候选词总数 < 20%（即重叠率 ≥ 80%）
  - 已完成 4 轮
```

#### 差评区挖掘（每条笔记顺带执行）

```bash
# 拉取评论（前 20 条）
mcporter call 'xiaohongshu.get_note_comments(note_id: "{id}")'
```

提取含以下词的评论：`避坑、踩坑、不靠谱、投诉、差评、后悔、骗、黑心、注意`

- 将提取的关键词标注 **A4**，加入长尾关键词库（`source: "comment"`）
- 涉及笔记记录为差评来源，列入 A4 爆款参考暂存

#### 关键词 A层标注规则

提取新词时按来源自动标注（若含信号词则用信号词判断，否则用默认值）：

| 来源 | 默认 A层 |
|------|---------|
| Round 1 提取 | A2 |
| Round 2 提取 | A2 |
| Round 3-4 提取 | A3 |
| 差评区提取 | A4（强制，不覆盖） |

所有词加入**长尾关键词库**，记录：`{词, a_layer, search_round, source: "content|comment"}`

---

### Step 3: 信息饱和检验 + 爆款笔记存档

对每条笔记在写入爆款参考前执行饱和检验：

#### 饱和检验规则

```
每个 A层 维护一个「已覆盖主题列表」（1-2 句话描述每个主题）。

存入前：
  1. 提取该笔记的核心主题（1-2 句话）
  2. 与已覆盖主题列表比较重叠度：
     - 重叠度 ≥ 80% → 跳过（不存，主题已覆盖）
     - 全新角度 → 存入，更新已覆盖主题列表
  3. 单层上限：20 条（达到后该层停止新增）
```

#### 获取笔记详情

对通过饱和检验的笔记，调用详情接口：
```bash
mcporter call 'xiaohongshu.get_feed_detail(feed_id: "{id}", xsec_token: "{xsecToken}")'
```

提取：标题、正文全文、标签、图片 URL、视频 URL 和时长、点赞/收藏/评论/分享、作者、精选评论（前 5 条）

#### 存为爆款参考笔记

路径：`素材库/4-爆款参考/小红书/{标题}.md`

**原文链接格式**（必须带 xsec_token）：
```
https://www.xiaohongshu.com/explore/{noteId}?xsec_token={xsecToken}&xsec_source=pc_feed
```

笔记模板：
```markdown
---
type: 爆款参考
platform: 小红书
note_id: {noteId}
original_url: https://www.xiaohongshu.com/explore/{noteId}?xsec_token={xsecToken}&xsec_source=pc_feed
date_collected: {日期}
note_type: {图文/视频}
author: {作者}
author_id: {作者ID}
likes: {点赞}
collects: {收藏}
comments: {评论}
shares: {分享}
a_layer: {A1-A5}
search_round: {1-4}
keyword_source: {触发这条结果的关键词}
tags:
  - 爆款参考
  - 小红书
used_count: 0
status: 未使用
---

# {标题}

## 互动数据
- 点赞 {点赞} / 收藏 {收藏} / 评论 {评论} / 分享 {分享}

## 正文
{完整正文}

## 标签
{提取的标签}

## 图片
{每张图 ![图N](url)}

## 视频（如有）
- 时长 / 地址

## 精选评论
{前 5 条}

## 内容拆解
{AI 分析：标题公式、Hook、结构、CTA}

## 为什么火
{AI 分析核心原因}

## 我能怎么用
{AI 提供应用建议}
```

---

### Step 4: 生成 4 张输出表

#### 4a. 长尾关键词库

存为 `素材库/8-长尾关键词库/{日期}-{业务}-关键词库.md`

```markdown
---
type: 长尾关键词库
business: {业务}
date: {日期}
total_keywords: {总数}
rounds_completed: {完成轮次}
stop_reason: {达到4轮 / 关键词重叠率≥80%}
---

# 长尾关键词库：{业务}

## 关键词总览

| 关键词 | A层 | 搜索轮次 | 来源 | 备注 |
|--------|-----|---------|------|------|
| 月嫂怎么选 | A3 | R1 | 内容 | |
| 月嫂避坑 | A4 | R2 | 内容+差评 | |

## 按 A层 分组

### A0（已过滤，不相关）：{N} 个词
{仅列词，不展开}

### A1（认知启蒙）：{N} 个词
{词列表}

### A2（研究阶段）：{N} 个词
{词列表}

### A3（对比选择）：{N} 个词
{词列表}

### A4（决策顾虑）：{N} 个词
{词列表，含差评区来源词}

### A5（准备成交）：{N} 个词
{词列表}
```

#### 4b. 客户画像表

存为 `素材库/9-客户画像/{日期}-{业务}-客户画像.md`

用以下 prompt 生成：

```
你是一个用户研究专家，擅长从搜索行为反推用户心理。

根据以下数据，分析该业务在每个决策阶段的用户特征：
- 各 A层 关键词列表（见长尾关键词库）
- 各 A层 爆款笔记的标题和高赞评论

请逐 A层 输出客户画像，每层包含：
1. 该阶段用户在想什么（显性问题/显性需求）
2. 该阶段用户真正担心什么（隐性情绪——他们搜索但不敢发朋友圈的困扰）
3. 该阶段的典型搜索场景（什么时候、什么情境下搜索这个词）
4. 该阶段最有效的内容钩子（什么样的标题最能吸引他们点击）
```

输出格式：
```markdown
---
type: 客户画像
business: {业务}
date: {日期}
---

# 客户画像表：{业务}

## A1 用户（认知启蒙阶段）
- **在想什么**：
- **真正担心**：
- **典型场景**：
- **最有效钩子**：

## A2 用户（研究阶段）
...

## A3 用户（对比选择阶段）
...

## A4 用户（决策顾虑阶段）
...

## A5 用户（准备成交阶段）
...
```

#### 4c. 对标爆款笔记库（选题报告中的汇总视图）

在选题报告中生成按 A层 分组的汇总视图（实体文件已在 Step 3 存入 `素材库/4-爆款参考/小红书/`）：

```markdown
## 对标爆款笔记库（按 A层）

### A1 爆款（{N} 条）
| 标题 | 赞 | 关键词来源 | 链接 |
|------|----|-----------|------|
| [[笔记名]] | 1200 | 月嫂 | ... |

### A2 爆款（{N} 条）
...

### A3 爆款（{N} 条）
...

### A4 爆款（{N} 条）
...

### A5 爆款（{N} 条）
...
```

#### 4d. 选题库

存为 `素材库/7-选题库/{日期}-{业务}-选题库.md`

```markdown
---
type: 选题库
business: {业务}
date: {日期}
---

# 选题库：{业务}

## 内容矩阵配比建议

基于关键词分布和各层竞争烈度（AI 根据实际数据调整比例）：

| A层 | 建议比例 | 数量（以 30 条/月为例） | 核心目的 |
|-----|---------|----------------------|---------|
| A1 | 20% | 6 条 | 拉新认知 |
| A2 | 30% | 9 条 | 建立信任 |
| A3 | 25% | 7 条 | 对比胜出 |
| A4 | 15% | 5 条 | 打消顾虑 |
| A5 | 10% | 3 条 | 直接转化 |

## 选题方向（按 A层）

### A1 选题（认知启蒙）
1. {选题方向} — 参考：[[爆款笔记名]] — 切入角度：{角度}
2. ...

### A2 选题（研究阶段）
...

### A3 选题（对比选择）
...

### A4 选题（决策顾虑）
...

### A5 选题（准备成交）
...
```

---

### Step 5: 写选题报告

路径：`素材库/5-选题报告/{日期}-{关键词}-小红书爆款分析.md`

```markdown
---
type: 选题报告
platform: 小红书
keyword: {初始关键词}
date: {日期}
rounds_completed: {N}
stop_reason: {达到4轮 / 关键词重叠率≥80%}
total_keywords: {长尾关键词总数}
total_notes: {收录爆款笔记总数}
tags:
  - 选题报告
  - 小红书
---

# 选题报告：{关键词} — 小红书爆款分析

## 搜索信息
- 日期 / 平台 / 初始关键词 / 完成轮次 / 停止原因

## 关键词分布
| A层 | 词数 | 占比 | 代表词 |
|-----|------|------|--------|

## 对标爆款笔记库
{见 Step 4c 的汇总视图}

## 竞争烈度分析
{AI 分析：哪些 A层 内容竞争激烈，哪些有空白机会}

## 相关文件
- 长尾关键词库：[[{日期}-{业务}-关键词库]]
- 客户画像表：[[{日期}-{业务}-客户画像]]
- 选题库：[[{日期}-{业务}-选题库]]
```

---

### Step 6: 输出汇总

```
=== 选题侦察完成 ===

搜索轮次：{N} 轮（搜索了 {M} 个关键词）
停止原因：{达到 4 轮 / 关键词重叠率 ≥ 80%}

关键词库：{总词数} 个
  A0（过滤）: {N} 个 | A1: {N} 个 | A2: {N} 个 | A3: {N} 个 | A4: {N} 个 | A5: {N} 个

爆款笔记：{总条数} 条
  A1: {N} 条 | A2: {N} 条 | A3: {N} 条 | A4: {N} 条 | A5: {N} 条

✅ 已存入 Obsidian：
  - 爆款参考    → 素材库/4-爆款参考/小红书/（{N} 条）
  - 选题报告    → 素材库/5-选题报告/
  - 选题库      → 素材库/7-选题库/
  - 长尾关键词库 → 素材库/8-长尾关键词库/
  - 客户画像表  → 素材库/9-客户画像/

=========================
```

---

## 快捷用法

- "帮我搜小红书上上海家政的爆款" → 直接跳到 Step 2
- "分析一下这些内容哪个方向好" → 跳到 Step 4
- "给我看关键词分布" → 输出 Step 4a 的 A层分组
